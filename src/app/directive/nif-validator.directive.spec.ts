import { NifValidatorDirective } from './nif-validator.directive';

describe('NifValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NifValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
